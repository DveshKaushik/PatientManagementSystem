services:
  app:
    build: .
    ports:
      - "8080:80"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ConnectionStrings__DefaultConnection=Server=host.docker.internal\SQLEXPRESS;Database=PatientManagementDB;User ID=sa;Password=YourStr0ngPassword!;TrustServerCertificate=True